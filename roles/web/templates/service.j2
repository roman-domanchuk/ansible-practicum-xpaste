[Unit]
Description=XPaste Practicum Service
After=network.target

[Service]
Type=simple
User=root
EnvironmentFile=/root/xpaste_practicum/.env
WorkingDirectory=/root/xpaste_practicum
ExecStart=/bin/bash -lc 'source /etc/profile.d/rvm.sh && bundle exec puma -b unix:///var/run/puma.sock -e $RAILS_ENV config.ru'
Restart=always

[Install]
WantedBy=multi-user.target
